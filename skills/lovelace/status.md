# Lovelace Status Skill

## Description
Provides information about the Lovelace's current state and available tools.

## Procedure
1. Use `/lovelace status` to see the current status.
2. Check `~/.lovelace/config.toml` for adapter settings.
3. Check `~/.lovelace/policies.toml` for active policies.
