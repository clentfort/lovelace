# Work Agent Status Skill

## Description
Provides information about the Work Agent's current state and available tools.

## Procedure
1. Use `/work-agent status` to see the current status.
2. Check `~/.work-agent/config.toml` for adapter settings.
3. Check `~/.work-agent/policies.toml` for active policies.
